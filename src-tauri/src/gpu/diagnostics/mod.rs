// gpu/diagnostics/mod.rs
// Tüketiciler `diagnostics::types::...` / `diagnostics::report::...` tam yollarını
// (veya gpu/mod.rs'teki üst düzey re-export'ları) kullanır; buradaki glob'lar gereksiz.
pub mod types;
pub mod report;
